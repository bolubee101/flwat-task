let body={
    // body to send
  }
let response={
    // expected body of response
  }
  // replace with expected status code in integer
  let status=200;

module.exports.body=body;
module.exports.response=response;
module.exports.status=status;